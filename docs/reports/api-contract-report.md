# Отчёт по API-контракту

## Источники

| Источник | Использование |
|---|---|
| `openapi-2.yaml` | список endpoints, DTO, статусы ответов |
| контроллеры | фактический routing и `@PreAuthorize` |
| сервисы | бизнес-правила и ошибки |
| security layer | JWT и роли |

## Покрытие

Все 43 HTTP операции из `openapi-2.yaml` отражены в [../api/endpoints.md](../api/endpoints.md).

## Расхождения

| Область | Контракт | Реализация |
|---|---|---|
| `DELETE /podcasts/{podcastId}/vote` | допускается `204`, если голос отсутствовал | возвращается `200 OK` с `VoteResponse` |

> Требует уточнения: финальное решение по статусу ответа для удаления отсутствующего голоса.

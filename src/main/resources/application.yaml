spring:
  application:
    name: podcastService

  datasource:
    url: ${PODCAST_DB_URL:jdbc:postgresql://localhost:5432/podcast_db}
    username: ${PODCAST_DB_USER:podcast_user}
    password: ${PODCAST_DB_PASSWORD:podcast_pass}
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  kafka:
    bootstrap-servers: ${PODCAST_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: ${PODCAST_KAFKA_CONSUMER_GROUP:podcast-service}
      auto-offset-reset: earliest
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "podcastService.infrastructure.messaging.event,podcastService.user.dto"
        spring.json.use.type.headers: false
        spring.json.value.default.type: "podcastService.infrastructure.messaging.event.EventEnvelope"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

app:
  cors:
    allowed-origins: ${PODCAST_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:8082}
  auth:
    jwt:
      enabled: ${PODCAST_AUTH_ENABLED:true}
      secret: ${PODCAST_ACCESS_TOKEN_SECRET:}
      issuer: ${PODCAST_ACCESS_TOKEN_ISSUER:auth-service}
  kafka:
    topics:
      users: ${PODCAST_KAFKA_TOPIC_USERS:podcasts.users}
      podcasts: ${PODCAST_KAFKA_TOPIC_PODCASTS:podcasts.podcasts}
    routing:
      user.created: users

server:
  port: ${PODCAST_SERVER_PORT:8082}
  servlet:
    context-path: ${PODCAST_SERVER_SERVLET_CONTEXT_PATH:/podcast/v1}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    org.flywaydb: INFO
    org.springframework.kafka: INFO

springdoc:
  api-docs:
    enabled: ${PODCAST_SWAGGER_ENABLED:false}
  swagger-ui:
    enabled: ${PODCAST_SWAGGER_ENABLED:false}
    path: /swagger
    url: ${PODCAST_SWAGGER_OPENAPI_URL:/openapi/podcast-service-dev.yaml}
    disable-swagger-default-url: true
    tryItOutEnabled: true

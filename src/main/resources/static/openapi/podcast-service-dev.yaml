openapi: 3.0.3

info:
  title: Podcast Service API (DEV)
  version: 1.3.0-dev
  description: |
    DEV-спецификация отражает текущую реализованную часть сервиса.

    Авторизация выполняется через `Authorization: Bearer <access_token>`.
    Access token выдаётся auth-service и валидируется локально как HS256 JWT.
    Используемые claims: `user_id`, `email`, `roles`, `iss`, `exp`.

    Роли:
    - `ADMIN` — управление категориями.
    - `AUTHOR` — профиль автора, создание, изменение, удаление и публикация подкастов.
    - авторизованный пользователь — профиль, настройки, плейлисты и голосование.

servers:
  - url: http://localhost:8082
    description: Local development

security:
  - bearerAuth: []

tags:
  - name: User Profile
    description: Профиль пользователя и настройки
  - name: Categories
    description: Категории подкастов
  - name: Podcasts
    description: CRUD и базовый жизненный цикл подкастов
  - name: Author Profile
    description: Профили авторов и публичные страницы авторов
  - name: Playlists
    description: Плейлисты и управление треклистом
  - name: Votes
    description: Голосование за плейлисты

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT access token, полученный от auth-service

  parameters:
    PathCategoryId:
      name: categoryId
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PathPodcastId:
      name: podcastId
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PathPlaylistId:
      name: playlistId
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PathAuthorId:
      name: authorId
      in: path
      required: true
      schema:
        type: string
        format: uuid

    Page:
      name: page
      in: query
      required: false
      description: Номер страницы, начиная с 1
      schema:
        type: integer
        minimum: 1
        default: 1

    Size:
      name: size
      in: query
      required: false
      description: Размер страницы, максимум 50
      schema:
        type: integer
        minimum: 1
        maximum: 50
        default: 20

  schemas:
    ErrorResponse:
      type: object
      required: [code, message, timestamp]
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Request validation failed
        timestamp:
          type: string
          format: date-time
        details:
          type: object
          nullable: true
          additionalProperties: true

    Theme:
      type: string
      enum: [DARK, LIGHT]

    Language:
      type: string
      enum: [RU, EN]

    VoteType:
      type: string
      enum: [LIKE, DISLIKE]

    PodcastStatus:
      type: string
      enum: [DRAFT, PROCESSING, PUBLISHED, FAILED, ARCHIVED]

    SortPodcasts:
      type: string
      enum: [DATE_DESC, DATE_ASC, RATING, VIEWS]
      default: DATE_DESC

    SortPlaylists:
      type: string
      enum: [DATE_DESC, DATE_ASC, RATING]
      default: DATE_DESC

    PageMeta:
      type: object
      required: [page, size, totalElements, totalPages]
      properties:
        page:
          type: integer
          example: 1
        size:
          type: integer
          example: 20
        totalElements:
          type: integer
          format: int64
          example: 53
        totalPages:
          type: integer
          example: 3

    UserProfileResponse:
      type: object
      required: [id, userId, username, createdAt]
      properties:
        id:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        username:
          type: string
          example: john_doe
        avatarUrl:
          type: string
          format: uri
          nullable: true
        createdAt:
          type: string
          format: date-time

    UserProfilePrivateResponse:
      allOf:
        - $ref: '#/components/schemas/UserProfileResponse'
        - type: object
          properties:
            theme:
              $ref: '#/components/schemas/Theme'
            language:
              $ref: '#/components/schemas/Language'

    UpdateUserProfileRequest:
      type: object
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 50
          pattern: '^[a-zA-Z0-9_]+$'
          example: john_doe
        avatarUrl:
          type: string
          format: uri
          nullable: true

    UpdateUserSettingsRequest:
      type: object
      properties:
        theme:
          $ref: '#/components/schemas/Theme'
        language:
          $ref: '#/components/schemas/Language'

    UserSettingsResponse:
      type: object
      properties:
        theme:
          $ref: '#/components/schemas/Theme'
        language:
          $ref: '#/components/schemas/Language'

    AuthorProfileResponse:
      type: object
      required: [id, userId, authorName, subscribersCount, createdAt]
      properties:
        id:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        authorName:
          type: string
          example: Иван Петров
        avatarUrl:
          type: string
          format: uri
          nullable: true
          description: Берётся из связанного user_profiles.avatar_url
        description:
          type: string
          nullable: true
        subscribersCount:
          type: integer
          format: int64
          example: 12400
        isSubscribed:
          type: boolean
          nullable: true
          description: null для анонимного запроса, true/false для авторизованного
        createdAt:
          type: string
          format: date-time

    CreateAuthorProfileRequest:
      type: object
      required: [authorName]
      properties:
        authorName:
          type: string
          minLength: 2
          maxLength: 100
          example: Иван Петров
        description:
          type: string
          maxLength: 1000
          nullable: true
          example: Рассказываю о технологиях доступным языком

    UpdateAuthorProfileRequest:
      type: object
      properties:
        authorName:
          type: string
          minLength: 2
          maxLength: 100
          example: Иван Петров
        description:
          type: string
          maxLength: 1000
          nullable: true

    CategoryResponse:
      type: object
      required: [id, name, position]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: Технологии
        position:
          type: integer
          example: 1

    CreateCategoryRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 100
          example: Технологии
        position:
          type: integer
          minimum: 0
          example: 1

    UpdateCategoryRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 100
          example: Бизнес
        position:
          type: integer
          minimum: 0
          example: 2

    AuthorCard:
      type: object
      required: [id, authorName, subscribersCount]
      properties:
        id:
          type: string
          format: uuid
        authorName:
          type: string
          example: Иван Петров
        avatarUrl:
          type: string
          format: uri
          nullable: true
        subscribersCount:
          type: integer
          format: int64
          nullable: true
          example: 125
        isSubscribed:
          type: boolean
          nullable: true
          example: false

    PodcastCard:
      type: object
      required: [id, title, author, status, viewsCount, likesCount, dislikesCount, createdAt]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
          example: Как построить первый продовый backend
        author:
          $ref: '#/components/schemas/AuthorCard'
        category:
          allOf:
            - $ref: '#/components/schemas/CategoryResponse'
          nullable: true
        coverImageUrl:
          type: string
          format: uri
          nullable: true
        durationSeconds:
          type: integer
          nullable: true
          example: 1830
        status:
          $ref: '#/components/schemas/PodcastStatus'
        viewsCount:
          type: integer
          format: int64
          example: 1500
        likesCount:
          type: integer
          format: int64
          example: 220
        dislikesCount:
          type: integer
          format: int64
          example: 4
        publishedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        currentUserVote:
          allOf:
            - $ref: '#/components/schemas/VoteType'
          nullable: true
        progressSeconds:
          type: integer
          format: int64
          nullable: true
        progressPercent:
          type: integer
          nullable: true
          minimum: 0
          maximum: 100

    PodcastDetailResponse:
      allOf:
        - $ref: '#/components/schemas/PodcastCard'
        - type: object
          properties:
            description:
              type: string
              nullable: true
              example: Обсуждаем архитектуру сервиса и типичные ошибки при проектировании.
            audioUrl:
              type: string
              format: uri
              nullable: true
            hasTranscript:
              type: boolean
              nullable: true
              example: false
            hasSummary:
              type: boolean
              nullable: true
              example: false

    CreatePodcastRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 3
          maxLength: 255
          example: Как построить первый продовый backend
        description:
          type: string
          maxLength: 5000
          nullable: true
        categoryId:
          type: string
          format: uuid
          nullable: true
        coverImageUrl:
          type: string
          format: uri
          nullable: true

    UpdatePodcastRequest:
      type: object
      description: Partial update. Должно быть передано хотя бы одно поле.
      properties:
        title:
          type: string
          minLength: 3
          maxLength: 255
        description:
          type: string
          maxLength: 5000
          nullable: true
        categoryId:
          type: string
          format: uuid
          nullable: true
        coverImageUrl:
          type: string
          format: uri
          nullable: true

    PlaylistOwnerResponse:
      type: object
      required: [id, username]
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
          example: john_doe

    PlaylistCard:
      type: object
      required: [id, title, owner, isPublic, podcastsCount, likesCount, dislikesCount, createdAt]
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
          example: Лучшие подкасты про IT
        coverImageUrl:
          type: string
          format: uri
          nullable: true
        owner:
          $ref: '#/components/schemas/PlaylistOwnerResponse'
        isPublic:
          type: boolean
        podcastsCount:
          type: integer
          format: int64
          example: 8
        likesCount:
          type: integer
          format: int64
          example: 55
        dislikesCount:
          type: integer
          format: int64
          example: 2
        createdAt:
          type: string
          format: date-time
        currentUserVote:
          allOf:
            - $ref: '#/components/schemas/VoteType'
          nullable: true

    PlaylistPodcastItem:
      allOf:
        - $ref: '#/components/schemas/PodcastCard'
        - type: object
          required: [position]
          properties:
            position:
              type: integer
              minimum: 1

    PlaylistDetailResponse:
      allOf:
        - $ref: '#/components/schemas/PlaylistCard'
        - type: object
          properties:
            description:
              type: string
              nullable: true
            updatedAt:
              type: string
              format: date-time
            podcasts:
              type: array
              items:
                $ref: '#/components/schemas/PlaylistPodcastItem'

    CreatePlaylistRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 255
        description:
          type: string
          maxLength: 1000
          nullable: true
        coverImageUrl:
          type: string
          format: uri
          nullable: true
        isPublic:
          type: boolean
          default: false

    UpdatePlaylistRequest:
      type: object
      description: Partial update. Должно быть передано хотя бы одно поле.
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 255
        description:
          type: string
          maxLength: 1000
          nullable: true
        coverImageUrl:
          type: string
          format: uri
          nullable: true
        isPublic:
          type: boolean

    AddPodcastToPlaylistRequest:
      type: object
      required: [podcastId]
      properties:
        podcastId:
          type: string
          format: uuid

    ReorderPlaylistRequest:
      type: object
      required: [items]
      properties:
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [podcastId, position]
            properties:
              podcastId:
                type: string
                format: uuid
              position:
                type: integer
                minimum: 1

    VoteRequest:
      type: object
      required: [voteType]
      properties:
        voteType:
          $ref: '#/components/schemas/VoteType'

    VoteResponse:
      type: object
      required: [targetId, targetType, likesCount, dislikesCount, currentUserVote]
      properties:
        targetId:
          type: string
          format: uuid
        targetType:
          type: string
          enum: [PLAYLIST]
        likesCount:
          type: integer
          format: int64
        dislikesCount:
          type: integer
          format: int64
        currentUserVote:
          allOf:
            - $ref: '#/components/schemas/VoteType'
          nullable: true

    PageOfPodcastCard:
      type: object
      required: [items, meta]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PodcastCard'
        meta:
          $ref: '#/components/schemas/PageMeta'

    PageOfPlaylistCard:
      type: object
      required: [items, meta]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PlaylistCard'
        meta:
          $ref: '#/components/schemas/PageMeta'

  responses:
    BadRequest:
      description: Невалидные входные данные
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    Unauthorized:
      description: Токен отсутствует, невалиден или истёк
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    Forbidden:
      description: Недостаточно прав или пользователь не владелец ресурса
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    NotFound:
      description: Ресурс не найден
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    Conflict:
      description: Конфликт данных
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

    UnprocessableEntity:
      description: Бизнес-правило нарушено
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

paths:
  /users/me/profile:
    get:
      tags: [User Profile]
      summary: Получить свой профиль
      responses:
        '200':
          description: Профиль пользователя
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfilePrivateResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags: [User Profile]
      summary: Обновить свой профиль
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserProfileRequest'
      responses:
        '200':
          description: Обновлённый профиль
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfilePrivateResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /users/me/settings:
    get:
      tags: [User Profile]
      summary: Получить настройки пользователя
      responses:
        '200':
          description: Настройки пользователя
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettingsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags: [User Profile]
      summary: Обновить настройки пользователя
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserSettingsRequest'
      responses:
        '200':
          description: Обновлённые настройки
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettingsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /users/me/playlists:
    get:
      tags: [Playlists]
      summary: Мои плейлисты
      responses:
        '200':
          description: Плейлисты текущего пользователя
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageOfPlaylistCard'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /authors/me:
    post:
      tags: [Author Profile]
      summary: Создать профиль автора
      description: Требуется роль `AUTHOR`. Пользовательский профиль должен уже существовать в `user_profiles`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAuthorProfileRequest'
      responses:
        '201':
          description: Профиль автора создан
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthorProfileResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

    get:
      tags: [Author Profile]
      summary: Свой профиль автора
      description: Требуется роль `AUTHOR`.
      responses:
        '200':
          description: Профиль автора
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthorProfileResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags: [Author Profile]
      summary: Обновить профиль автора
      description: Partial update. `avatarUrl` обновляется через `PUT /users/me/profile`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAuthorProfileRequest'
      responses:
        '200':
          description: Обновлённый профиль автора
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthorProfileResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /authors/{authorId}:
    get:
      tags: [Author Profile]
      summary: Публичный профиль автора
      description: Публичная ручка. Если передан валидный Bearer token, вернёт `isSubscribed`.
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PathAuthorId'
      responses:
        '200':
          description: Профиль автора
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthorProfileResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /authors/{authorId}/podcasts:
    get:
      tags: [Author Profile]
      summary: Опубликованные подкасты автора
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PathAuthorId'
        - name: q
          in: query
          schema: { type: string }
        - name: sort
          in: query
          schema:
            $ref: '#/components/schemas/SortPodcasts'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: Страница опубликованных подкастов автора
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageOfPodcastCard'
        '404':
          $ref: '#/components/responses/NotFound'

  /categories:
    get:
      tags: [Categories]
      summary: Список всех категорий
      security: []
      responses:
        '200':
          description: Список категорий
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CategoryResponse'

    post:
      tags: [Categories]
      summary: Создать категорию
      description: Требуется роль `ADMIN`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryRequest'
      responses:
        '201':
          description: Категория создана
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /categories/{categoryId}:
    put:
      tags: [Categories]
      summary: Обновить категорию
      description: Требуется роль `ADMIN`.
      parameters:
        - $ref: '#/components/parameters/PathCategoryId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCategoryRequest'
      responses:
        '200':
          description: Обновлённая категория
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags: [Categories]
      summary: Удалить категорию
      description: Требуется роль `ADMIN`.
      parameters:
        - $ref: '#/components/parameters/PathCategoryId'
      responses:
        '204':
          description: Категория удалена
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /podcasts:
    get:
      tags: [Podcasts]
      summary: Список опубликованных подкастов
      description: Публичная ручка. Если передан валидный Bearer token, ответ может учитывать текущего пользователя.
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          schema: { type: string }
        - name: categoryId
          in: query
          schema:
            type: string
            format: uuid
        - name: authorId
          in: query
          schema:
            type: string
            format: uuid
        - name: sort
          in: query
          schema:
            $ref: '#/components/schemas/SortPodcasts'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: Страница с подкастами
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageOfPodcastCard'

    post:
      tags: [Podcasts]
      summary: Создать подкаст
      description: Требуется роль `AUTHOR`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePodcastRequest'
      responses:
        '201':
          description: Подкаст создан
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PodcastDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /podcasts/{podcastId}:
    get:
      tags: [Podcasts]
      summary: Получить детальную информацию о подкасте
      description: Публично доступен только `PUBLISHED`; автор-владелец может увидеть свой неопубликованный подкаст с Bearer token.
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PathPodcastId'
      responses:
        '200':
          description: Детальная карточка подкаста
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PodcastDetailResponse'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags: [Podcasts]
      summary: Обновить подкаст
      description: Требуется роль `AUTHOR`; менять может только автор-владелец.
      parameters:
        - $ref: '#/components/parameters/PathPodcastId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePodcastRequest'
      responses:
        '200':
          description: Подкаст обновлён
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PodcastDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

    delete:
      tags: [Podcasts]
      summary: Архивировать подкаст
      description: Требуется роль `AUTHOR`; архивировать может только автор-владелец.
      parameters:
        - $ref: '#/components/parameters/PathPodcastId'
      responses:
        '204':
          description: Подкаст архивирован
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /podcasts/{podcastId}/publish:
    post:
      tags: [Podcasts]
      summary: Отправить подкаст в обработку
      description: Требуется роль `AUTHOR`; публиковать может только автор-владелец.
      parameters:
        - $ref: '#/components/parameters/PathPodcastId'
      responses:
        '202':
          description: Подкаст принят в обработку
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PodcastDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /authors/{authorId}/playlists:
    get:
      tags: [Author Profile]
      summary: Публичные плейлисты автора
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PathAuthorId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: Страница плейлистов автора
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageOfPlaylistCard'
        '404':
          $ref: '#/components/responses/NotFound'

  /playlists:
    get:
      tags: [Playlists]
      summary: Публичные плейлисты
      description: Публичная ручка. Если передан валидный Bearer token, вернёт `currentUserVote`.
      security:
        - {}
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          schema: { type: string }
        - name: sort
          in: query
          schema:
            $ref: '#/components/schemas/SortPlaylists'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
      responses:
        '200':
          description: Страница публичных плейлистов
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageOfPlaylistCard'

    post:
      tags: [Playlists]
      summary: Создать плейлист
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePlaylistRequest'
      responses:
        '201':
          description: Плейлист создан
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /playlists/{playlistId}:
    get:
      tags: [Playlists]
      summary: Получить плейлист с треклистом
      description: Публичный плейлист доступен всем; приватный доступен только владельцу с Bearer token.
      security:
        - {}
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/PathPlaylistId'
      responses:
        '200':
          description: Плейлист с треклистом
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistDetailResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags: [Playlists]
      summary: Обновить плейлист
      description: Только владелец.
      parameters:
        - $ref: '#/components/parameters/PathPlaylistId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePlaylistRequest'
      responses:
        '200':
          description: Обновлённый плейлист
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags: [Playlists]
      summary: Удалить плейлист
      description: Только владелец.
      parameters:
        - $ref: '#/components/parameters/PathPlaylistId'
      responses:
        '204':
          description: Плейлист удалён
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /playlists/{playlistId}/podcasts:
    post:
      tags: [Playlists]
      summary: Добавить подкаст в плейлист
      description: Только владелец. Можно добавить только `PUBLISHED` подкаст.
      parameters:
        - $ref: '#/components/parameters/PathPlaylistId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddPodcastToPlaylistRequest'
      responses:
        '200':
          description: Подкаст добавлен
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /playlists/{playlistId}/podcasts/{podcastId}:
    delete:
      tags: [Playlists]
      summary: Убрать подкаст из плейлиста
      description: Только владелец.
      parameters:
        - $ref: '#/components/parameters/PathPlaylistId'
        - $ref: '#/components/parameters/PathPodcastId'
      responses:
        '204':
          description: Подкаст убран
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /playlists/{playlistId}/podcasts/reorder:
    put:
      tags: [Playlists]
      summary: Изменить порядок подкастов в плейлисте
      description: Только владелец. Принимает полный новый порядок всех подкастов плейлиста.
      parameters:
        - $ref: '#/components/parameters/PathPlaylistId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderPlaylistRequest'
      responses:
        '200':
          description: Порядок обновлён
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistDetailResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /playlists/{playlistId}/vote:
    post:
      tags: [Votes]
      summary: Проголосовать за плейлист
      parameters:
        - $ref: '#/components/parameters/PathPlaylistId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VoteRequest'
      responses:
        '200':
          description: Голос засчитан
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoteResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags: [Votes]
      summary: Отозвать голос с плейлиста
      parameters:
        - $ref: '#/components/parameters/PathPlaylistId'
      responses:
        '200':
          description: Голос отозван
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VoteResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
